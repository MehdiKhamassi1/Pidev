{% extends 'basel.html.twig' %}

{% block title %}docteur  index{% endblock %}
 {% block body %}
 {% endblock %}
{% block content %}
    <h1>Docteur</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ docteur.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ docteur.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ docteur.prenom }}</td>
            </tr>
            <tr>
                <th>Date_naissance</th>
                <td>{{ docteur.date_de_naissance }}</td>
            </tr>
            <tr>
                <th>Specialite</th>
                <td>{{ docteur.specialite }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ docteur.email }}</td>
            </tr>
            
            <tr>
                <th>Numtel</th>
                <td>{{ docteur.numtel }}</td>
            </tr>
            <tr>
                <th>image</th>
                <td> <img src="{{ asset('uploads/docteur/' ~ docteur.profileImage) }}" alt="docteur Image" class="img-fluid mb-3" height="100px" width="100px"> </td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_home') }}">back to list</a>

    <a href="{{ path('app_docteur_edit', {'id': docteur.id}) }}">edit</a>

    {{ include('docteur/_delete_form.html.twig') }}
{% endblock %}
